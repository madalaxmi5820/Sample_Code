# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Gymondo' do
  use_frameworks!
  pod 'Kingfisher', '~> 7.0'

  # Pods for Gymondo

  target 'GymondoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GymondoUITests' do
    # Pods for testing
  end

end
